// a linked list is a series of connected nodes, each of which is a data structure
// grows or shrinks when the program runs
// nodes are dynamically allocated
// easily grows or shrinks in size, whereas arrays are fixed
// faster than vectors when inserting or deleting nodes from the list

#include <iostream>
using namespace std;

struct Node {
    int value;
    Node* next;
};

int main() {
    return 0;
}