#include <iostream>
using namespace std;

// insertion algorithm maintains an ascending order, based on a key
// makes the list an ordered list
// create a node, store data in the node, locate the insertion point, insert the node

int main() {
    // if list is empty, make the new node the first node
    /* else, find the first node whose value is >= new value, 
    or the end of the list (whichever is first)
    */
    /* initialize 2 pointers, prev (with memory address of head node) */

}